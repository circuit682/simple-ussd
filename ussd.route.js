const express = require('express')
const router = express.Router()
const {ussdRouter} = require('ussd-router')
// put,delete,update

router.get(ND,'/', async)(req,res,next);=>{
    try{
        const {sessionId, servercode}
    }
}